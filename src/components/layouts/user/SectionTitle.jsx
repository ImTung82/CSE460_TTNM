import React from 'react';

const SectionTitle = ({ title }) => (
  <div className="mb-4">
    <h2 className="text-sm font-semibold text-gray-800 uppercase border-b pb-1 border-gray-200">{title}</h2>
  </div>
);

export default SectionTitle;
